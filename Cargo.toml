[package]
name = "zero-to-rust-jit"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
console = "0.15.7"
libc = "0.2.151"
llvm-sys = { version = "170.0.1", features = ["prefer-dynamic"] }
